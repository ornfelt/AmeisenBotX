<Window x:Class="AmeisenBotX.LoadConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AmeisenBotX" Height="520" Width="680" MinHeight="400" MinWidth="520"
        WindowStyle="None" AllowsTransparency="True" BorderThickness="1" 
        Loaded="Window_Loaded" MouseLeftButtonDown="Window_MouseLeftButtonDown" 
        Icon="icon.png" ResizeMode="CanResizeWithGrip">
    
    <Window.BorderBrush>
        <SolidColorBrush Color="{DynamicResource DarkAccent1}" />
    </Window.BorderBrush>
    <Window.Background>
        <SolidColorBrush Color="{DynamicResource DarkBackground}" />
    </Window.Background>
    
    <Window.Resources>
        <!-- Close button hover style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFFF6500"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFCC4400"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Realm group header style -->
        <Style x:Key="RealmHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Bahnschrift Light"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#FFFF6500"/>
            <Setter Property="Margin" Value="12,12,12,6"/>
        </Style>
        
        <!-- Search box style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FF2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="#FFFF6500"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Title with orange robot icon -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <TextBlock Text="🤖" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"
                           Foreground="#FFFF6500"/>
                <TextBlock Text="AmeisenBotX" FontFamily="Bahnschrift Light" FontSize="18" VerticalAlignment="Center">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                    </TextBlock.Foreground>
                </TextBlock>
            </StackPanel>
            
            <!-- Close Button with hover effect -->
            <Button x:Name="buttonClose" Content="✕" Grid.Column="1"
                    Width="46" Height="40" 
                    FontFamily="Segoe UI" FontSize="14"
                    Style="{StaticResource CloseButtonStyle}" 
                    Click="ButtonClose_Click">
                <Button.Foreground>
                    <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                </Button.Foreground>
            </Button>
        </Grid>
        
        <!-- Header -->
        <Grid Grid.Row="1" Margin="16,12,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="Select Character" FontFamily="Bahnschrift" FontSize="22" FontWeight="SemiBold">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                    </TextBlock.Foreground>
                </TextBlock>
                <TextBlock x:Name="ProfileCountText" Text="0 profiles" 
                           FontFamily="Bahnschrift Light" FontSize="12" Margin="0,2,0,0" Opacity="0.6">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                    </TextBlock.Foreground>
                </TextBlock>
            </StackPanel>
            
            <!-- Search Box -->
            <Grid Grid.Column="1" Width="200" VerticalAlignment="Center">
                <TextBox x:Name="SearchBox" Style="{StaticResource SearchBoxStyle}"
                         TextChanged="SearchBox_TextChanged"/>
                <TextBlock x:Name="SearchPlaceholder" Text="🔍 Search..." IsHitTestVisible="False"
                           FontFamily="Bahnschrift Light" FontSize="12"
                           Foreground="#FF666666" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>
        
        <!-- Separator -->
        <Rectangle Grid.Row="2" Height="1" Margin="16,0,16,0">
            <Rectangle.Fill>
                <SolidColorBrush Color="{DynamicResource DarkBorder}" />
            </Rectangle.Fill>
        </Rectangle>
        
        <!-- Grouped Character Grid -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="0,8,0,8">
            <StackPanel x:Name="GroupedContainer"/>
        </ScrollViewer>
        
        <!-- Empty State (shown when no profiles exist) -->
        <StackPanel x:Name="EmptyState" Grid.Row="3" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Collapsed">
            <TextBlock Text="🎮" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="No bot profiles yet" FontFamily="Bahnschrift Light" FontSize="16" 
                       HorizontalAlignment="Center" Opacity="0.7">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                </TextBlock.Foreground>
            </TextBlock>
            <TextBlock Text="Click the + card to create your first bot" FontFamily="Bahnschrift Light" FontSize="12" 
                       HorizontalAlignment="Center" Opacity="0.5" Margin="0,4,0,0">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                </TextBlock.Foreground>
            </TextBlock>
        </StackPanel>
        
        <!-- No Search Results State -->
        <StackPanel x:Name="NoResultsState" Grid.Row="3" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Collapsed">
            <TextBlock Text="🔍" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <TextBlock Text="No matching profiles" FontFamily="Bahnschrift Light" FontSize="14" 
                       HorizontalAlignment="Center" Opacity="0.7">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{DynamicResource DarkForeground}" />
                </TextBlock.Foreground>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>